name: Auto linter2

on:
  push:
    paths: "*.py"

jobs:
  linting:
    runs-on: ubuntu-latest
    steps:
      - name: code checkout
        uses: actions/checkout@v7
      - name: Run pylint
        uses: dciborow/action-pylint@0.1.0
