name: CICD

on:
  workflow_dispatch:

jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: Clone the code
        run: echo "Cloning the code"
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build the code
        run: echo "Building the code"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Running the cases
        run: echo "Testing the app"
  deploy:
      runs-on: ubuntu-latest
    steps:
      - name: Deploy the code
        run: echo "Deploy the app"
